apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-server.fullname" . }}
  labels:
    {{- include "chat-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "chat-server.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
